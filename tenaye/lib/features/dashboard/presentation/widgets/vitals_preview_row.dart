import 'package:flutter/material.dart';

class VitalsPreviewRow extends StatelessWidget {
  const VitalsPreviewRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
